{
  "name": "cloud-chat-backend",
  "version": "1.0.0",
  "description": "Realtime chat backend: Socket.io + Redis (масштабирование) + Firebase (auth + хранение сообщений). Деплой на Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^8.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "redis": "^4.6.14",
    "socket.io": "^4.7.5"
  }
}
